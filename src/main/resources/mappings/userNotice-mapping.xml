<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NoticeDAO">
	<sql id="page" >
		select * from notice_board limit #{listSize} offset #{startList}
	</sql>

	<select id="noticein" parameterType="notice" resultType="notice">
		SELECT * FROM NOTICE_BOARD WHERE NO=#{no}
	</select>
	
	<select id="notice" parameterType="notice" resultType="notice">
		SELECT * FROM NOTICE_BOARD 
		WHERE TITLE LIKE '%'||#{searchKeyword}||'%'
		ORDER BY NO DESC
	</select>
	
	<select id="pagingList" resultType="notice">
		select * from notice_board limit #{listSize} offset #{startList}
	</select>
	
	<select id="searchPagingList" resultType="notice" >
		select * from notice_board where TITLE LIKE '%'||#{searchKeyword}||'%' limit #{listSize} offset #{startList}
	</select>
	
	<select id="pagingCnt" resultType="int">
		select count(*) as listCnt from notice_board
	</select>
	
	<select id="searchTitleCnt" parameterType="String" resultType="int">
		select count(*) from notice_board where TITLE LIKE '%'||#{searchKeyword}||'%';
	</select>
</mapper>